# requirements-defender.txt
# Dependencies for VM 310 / VM 320 (defender-a / defender-b): ML training + FL client + ETA extraction
# Deploy: pip install -r requirements-defender.txt
# Target host: Ubuntu 24.04 LTS, Python 3.11+
# Note: libpcap-dev must be installed at OS level first: apt install libpcap-dev tcpdump

# ── Deep Learning Framework ───────────────────────────────────
torch>=2.0,<3.0                   # CPU or CUDA build — see GPU section below

# ── Continual Learning ────────────────────────────────────────
avalanche-lib>=0.5,<1.0           # EWC, GEM strategies via Avalanche

# ── Federated Learning Client ─────────────────────────────────
flwr==1.32.1

# ── Differential Privacy ─────────────────────────────────────
opacus>=1.4,<2.0                  # DP-SGD gradient clipping + noise injection

# ── Encrypted Traffic Feature Extraction ─────────────────────
nfstream>=6.5,<7.0                # JA3/JA4, SPLT, flow-level ETA features (requires libpcap-dev)

# ── Numerical & Data Utilities ───────────────────────────────
numpy>=1.24,<3.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0            # StandardScaler, F1/precision/recall metrics

# ── Experiment Tracking (client-side logging) ────────────────
mlflow==3.15.1

# ── Visualization ─────────────────────────────────────────────
matplotlib>=3.7,<4.0
tensorboard>=2.13,<3.0

# ── ONNX Runtime (for INT8 / ONNX inference benchmarking) ────
onnxruntime>=1.17,<2.0
onnx>=1.15,<2.0

# ── Notification ─────────────────────────────────────────────
requests>=2.31,<3.0               # Telegram Bot API HTTP calls (notifications.py)

# ── Configuration ─────────────────────────────────────────────
pyyaml>=6.0,<7.0                  # experiment.yaml loading

# ── Optional: GPU-Accelerated Training ───────────────────────
# For GPU passthrough via vfio on Proxmox, install CUDA-enabled torch instead:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
